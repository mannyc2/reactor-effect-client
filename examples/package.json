{
  "name": "reactor-effect-examples",
  "version": "0.2.0",
  "private": true,
  "description": "Checked-in usage examples compiled against the workspace packages",
  "type": "module",
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "effect": "catalog:",
    "reactor-effect-browser": "workspace:*",
    "reactor-effect-client": "workspace:*",
    "reactor-effect-native": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:"
  },
  "scripts": {
    "typecheck": "tsc -p node/tsconfig.json --noEmit && tsc -p browser/tsconfig.json --noEmit && tsc -p tsconfig.check.json --noEmit",
    "check": "node check.mjs"
  }
}
